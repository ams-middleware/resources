name: dev

on:
  push:
    branches: [dev]

jobs:
  notify-api:
    name: Notificar a control-api (dev) para refrescar recursos
    runs-on: ubuntu-latest
    steps:
      - name: Update resources (dev)
        env:
          API_URL: "https://control-api.e-middleware.dev/notify/update-resources"
          APP_KEY: ${{ secrets.NOTIFY_APP_KEY }}
        run: |
          curl -X POST "$API_URL" \
            -H 'accept: application/json' \
            -H "app_key: $APP_KEY" \
            -d ''
